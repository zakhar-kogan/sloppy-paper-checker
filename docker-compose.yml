name: sloppy-paper-checker
services:
  gateway:
    build: ./web
    env_file: .env
    restart: unless-stopped
    ports: ["80:80", "443:443", "443:443/udp"]
    volumes:
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      api: {condition: service_started}
  document-init:
    build: ./backend
    user: root
    command: sh -c "chown -R app:app /app/data/documents"
    volumes: ["documents:/app/data/documents"]
    restart: "no"
  api:
    build: ./backend
    command: sh -c "alembic upgrade head && uvicorn sloppy_checker.main:app --host 0.0.0.0 --port 8787 --proxy-headers --forwarded-allow-ips=*"
    env_file: .env
    restart: unless-stopped
    environment:
      SPC_DOCUMENT_STORE: filesystem
      SPC_DOCUMENT_STORE_PATH: /app/data/documents
      SPC_ANALYSIS_DISPATCHER: inline
    ports: ["127.0.0.1:8787:8787"]
    volumes: ["documents:/app/data/documents"]
    depends_on:
      document-init: {condition: service_completed_successfully}
      postgres: {condition: service_healthy}
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: paper
      POSTGRES_PASSWORD: paper
      POSTGRES_DB: paper
    volumes: ["postgres:/var/lib/postgresql/data"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U paper"]
      interval: 5s
      timeout: 3s
      retries: 20
volumes:
  caddy_data:
  caddy_config:
  postgres:
  documents:
